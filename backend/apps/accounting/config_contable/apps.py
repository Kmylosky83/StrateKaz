from django.apps import AppConfig


class ConfigContableConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.accounting.config_contable'
    verbose_name = 'ConfigContable'
