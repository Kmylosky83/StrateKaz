# Generated by Django 5.0.9 on 2026-01-13 15:29

from django.db import migrations

def add_default_actions(apps, schema_editor):
    TabSection = apps.get_model('core', 'TabSection')
    # Update ALL sections to have custom actions for testing
    count = 0
    for section in TabSection.objects.all():
        section.supported_actions = ['enviar', 'aprobar']
        section.save()
        count += 1
    print(f"\nUpdated {count} sections with default actions.")

class Migration(migrations.Migration):

    dependencies = [
        ('core', '0009_add_crud_actions_to_section_access'),
    ]

    operations = [
        migrations.RunPython(add_default_actions),
    ]
